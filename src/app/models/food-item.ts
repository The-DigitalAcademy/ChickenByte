export interface FoodItem {
  id: string;
  title: string;
  description: string;
  price: number;
  category: string;
  image: string;
  promotion: string;
  flavour:string
  // Add other properties as needed
}